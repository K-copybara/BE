apiVersion: apps/v1
kind: Deployment
metadata:
  name: alarm-service
  namespace: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: alarm-service
  template:
    metadata:
      labels:
        app: alarm-service
    spec:
      containers:
        - name: alarm-service
          image: tabletalk.kr.ncr.ntruss.com/alarm-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8083
          envFrom:
            - configMapRef:
                name: alarm-config
            - secretRef:
                name: alarm-secret
            - secretRef:
                name: kafka-secret
          volumeMounts:
            - name: kafka-ca-cert
              mountPath: /etc/kafka/secrets
              readOnly: true
          resources:
            requests:
              cpu: 150m
              memory: 156Mi
            limits:
              cpu: 300m
              memory: 512Mi
      imagePullSecrets:
        - name: ncr-secret
      volumes:
        - name: kafka-ca-cert
          secret:
            secretName: kafka-cluster-cluster-ca-cert
  revisionHistoryLimit: 1
---
apiVersion: v1
kind: Service
metadata:
  name: alarm-service
  namespace: backend
spec:
  selector:
    app: alarm-service
  ports:
    - name: http
      port: 80
      targetPort: 8083
      nodePort: 30083
  type: NodePort
